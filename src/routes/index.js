import { Redirect, Route, Router } from "react-router-dom";

export const whiteList = [<Route key="dashboar" path="/" component="" exact />];
export const blackList = [<Route key="signin" path="/" component="" exact />];
